export enum WorkflowNodeType {
  TRIGGER = 'TRIGGER',
  HTTP = 'HTTP',
  WEBHOOK = 'WEBHOOK',
  DATABASE = 'DATABASE',
  DELAY = 'DELAY',
  NOTIFICATION = 'NOTIFICATION',
  FORM = 'FORM',
  IF = 'IF',
}
